<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.QueerMap" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

    </style>

    <style name="Theme.QueerMap.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_color</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_queermap_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.QueerMap</item>
    </style>



    <style name="Theme.QueerMap" parent="Base.Theme.QueerMap" />
</resources>